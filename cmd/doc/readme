## 1. 项目背景与目标

### 1.1 问题陈述

传统建筑能耗仿真（EnergyPlus）存在以下痛点：

- **IDF 配置门槛高**：EnergyPlus Input Data File（IDF）语法严苛，对象类型逾百种，非专业人员难以上手
- **调试困难**：仿真错误信息晦涩，根因定位耗时
- **结果分析依赖专家**：能耗报告解读需要建筑能耗领域知识，普通设计师难以独立完成
- **迭代效率低**：参数修改→重新仿真→结果对比的循环需要人工介入每个环节

### 1.2 系统目标

构建基于 LLM 的自动化建筑能耗仿真平台，实现：

1. 将非结构化自然语言建筑描述自动转化为可运行的 IDF 仿真模型
2. 仿真错误自动分类诊断与自愈修复（含人工审批门控）
3. 可选模块（RAG  / Skills）均支持独立启用/关闭

---

## 2. 系统分层架构

```
┌─────────────────────────────────────────────────────────┐
│                   用户 / CLI / MCP 客户端                 │
└───────────────────────────┬─────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────┐
│              编排层 (Orchestration Layer)                 │
│   MainGraph ──PhaseRouter──► Intent / Sim / Report 子图  │
│              各子图通过 AppContext 访问下层资源             │
└──────────┬────────────────┬────────────────┬────────────┘
           │                │                │
┌──────────▼──────┐  ┌──────▼──────┐  ┌──────▼──────────┐
│  Skills 工具层  │  │  RAG 检索层 │  │  MCP 执行层     │
│  SkillRegistry  │  │ RAGProvider │  │  ConverterMgr   │
│  按需查找       │  │ （各索引）  │  │  EnergyPlusRun  │
└─────────────────┘  └─────────────┘  └─────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────┐
│              基础设施层 (Infrastructure Layer)             │
│          VersionStore │ AppConfig │ Logging              │
└─────────────────────────────────────────────────────────┘
```