## 1. 项目背景与目标

### 1.1 问题陈述

传统建筑能耗仿真（EnergyPlus）存在以下痛点：

- **IDF 配置门槛高**：EnergyPlus Input Data File（IDF）语法严苛，对象类型逾百种，非专业人员难以上手
- **调试困难**：仿真错误信息晦涩，根因定位耗时
- **结果分析依赖专家**：能耗报告解读需要建筑能耗领域知识，普通设计师难以独立完成
- **迭代效率低**：参数修改→重新仿真→结果对比的循环需要人工介入每个环节

### 1.2 系统目标

构建基于 LLM 的自动化建筑能耗仿真平台，实现：

1. 将非结构化自然语言建筑描述自动转化为可运行的 IDF 仿真模型
2. 仿真错误自动分类诊断与自愈修复（含人工审批门控）
3. 可选模块（RAG  / Skills）均支持独立启用/关闭

---

## 2. 系统分层架构

```
┌─────────────────────────────────────────────────────────┐
│                   用户 / CLI / MCP 客户端                 │
└───────────────────────────┬─────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────┐
│              编排层 (Orchestration Layer)                 │
│          动态路由  ──► 意图理解 / 仿真模拟 / 报告解读    |
|                   状态协调中枢（状态机）                │
│               通过 AppContext 访问下层资源             │
└──────────┬────────────────┬────────────────┬────────────┘
           │                |                │         
┌──────────▼──────┐  ┌──────▼──────┐  ┌──────▼──────────┐
│  Skills 工具层  │  │  RAG 检索层 │  │  MCP 执行层     │
│  SkillRegistry  │  │ RAGProvider │  │  ConverterMgr   │
│  按需查找       │  │ （各索引）  │  │  EnergyPlusRun  │
└─────────────────┘  └─────────────┘  └─────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────┐
│              基础设施层 (Infrastructure Layer)             │
│          VersionStore │ AppConfig │ Logging              │
└─────────────────────────────────────────────────────────┘
```

注：注册层，分全局or 意图理解 / 仿真模拟 / 报告解读；按需调用

1. prompt管理、上下文管理该如何实现？
2. 报告解读需要引入plan-excute-replan形式？
3. 状态协调中枢该如何设计：idf路径、模拟结果路径、错误管理？

参数敏感性分析。 用户说"帮我优化能耗"，Agent 自主选择几个关键参数（窗墙比、外墙保温层厚度、HVAC 类型），分别做仿真对比，告诉用户"把窗墙比从 0.4 降到 0.3 能省 12% 能耗，但换 HVAC 系统只省 3%"。这个展示的是 Agent 的自主实验设计能力，非常加分。但前提是前三个先做好。

EnergyPlus 参数优化场景中提炼出了一个 Planner-Worker 架构模式——有状态的决策者负责实验设计和跨维度综合分析，无状态的 Worker 负责并行执行和局部自愈。这个模式可以泛化到任何需要多维度独立探索+全局综合决策的 Agent 场景



对 EnergyPlus 仿真错误和 YAML 验证错误做结构化分类，每类错误对应专用修复 prompt 模板，替代将原始报错盲目重试的朴素方案。



EnergyPlus Agent — 基于 LLM 的建筑能耗自动化仿真与优化平台
技术栈： Go / Python / LLM API / MCP Protocol / RAG (ChromaDB) / EnergyPlus
项目背景： EnergyPlus 建筑能耗仿真依赖专家手工编写 IDF 文件，语法零容忍、对象间深度耦合，从建模到优化严重依赖领域专家。
核心工作：
1. 模糊描述→完整建筑参数的自主推理。 用户输入非结构化建筑描述，Agent 自主规划推理路径（查气候区→检索节能规范→推导热工参数→选型 HVAC），逐步收敛为完整的仿真模型。构建 IDF 片段级 RAG，将验证通过的 IDF 按对象类型拆分入库做 few-shot 检索，一次生成通过率从 45% 提升至 78%。
2. 参数敏感性分析引擎（核心亮点）。 提出 Planner-Worker 架构：有状态的 Planner Agent 基于建筑特征自主选择高影响参数方向、设计多方向对比实验；无状态 Worker 并行执行各方向仿真，独立上下文互不污染，内置错误分类+定向修复+断路器机制。Planner 汇总结果做跨维度分析（效果/成本/可信度），自主决定是否追加组合实验。全闭环无需人工介入。
3. 构建双路混合检索系统（Hybrid RAG）：结合 BM25 稀疏检索与向量检索，设计结构感知切片策略（章节／条文／表格三粒度），确保建筑规范数值与上下文的完整关联。